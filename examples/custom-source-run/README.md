# Custom source run

This directory contains sanitized evidence from a live local HTTP run created
through the Creative Studio API using a custom synthetic source package:

```text
42 Signal Street, Testville
$710,000
4 beds · 3 baths · 2,400 sq ft
```

Observed state transition:

```text
NEEDS_REVIEW -> APPROVED
```

The run served a HyperFrames contact sheet through the local artifact endpoint.
External effects remained `NOT_ATTEMPTED`.
