# Multi-output local run

This directory contains sanitized evidence from a live local HTTP run that
requested two output targets from the same source package and creative brief:

```text
video + website
```

The run reached `NEEDS_REVIEW`, served both the HyperFrames contact sheet and a
source-bound website artifact, and then transitioned to `APPROVED` through the
local approval endpoint. External effects remained `NOT_ATTEMPTED`.
